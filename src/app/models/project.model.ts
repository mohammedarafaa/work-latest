export interface Area {
  nameAr: string;
  name: string;
  code: number;
}

export interface Project {
  name: string;
  address: string;
  image: string;
  area: Area;
  zone: string;
  code: number;
} 